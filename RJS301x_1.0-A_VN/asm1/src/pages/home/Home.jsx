import { City, Header } from "../../components";
import { Default } from "../../layouts";

const Home = () => {
  return (
    <Default>
      <Header />
      <City />
    </Default>
  );
};

export default Home;
