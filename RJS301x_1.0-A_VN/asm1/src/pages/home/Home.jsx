import { City, Header, Type } from "../../components";
import { Default } from "../../layouts";

const Home = () => {
  return (
    <Default>
      <Header />
      <City />
      <Type />
    </Default>
  );
};

export default Home;
